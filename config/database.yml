# PostgreSQL. Versions 9.3 and up are supported.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>


development:
  <<: *default
  database: roadstead_api_dev
  url: <%= ENV["ROADSTEAD_API_DEV_DATABASE_URL"] %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: roadstead_api_test
  url: <%= ENV["ROADSTEAD_API_DEV_DATABASE_URL"] %>

production:
  primary: &primary_production
    <<: *default
    database: roadstead_api
    url: <%= ENV["ROADSTEAD_API_PROD_DATABASE_URL"] %>
  cache:
    <<: *primary_production
    database: roadstead_api_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: roadstead_api_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: roadstead_api_cable
    migrations_paths: db/cable_migrate
